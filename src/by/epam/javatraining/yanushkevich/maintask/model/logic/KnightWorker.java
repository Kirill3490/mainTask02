package by.epam.javatraining.yanushkevich.maintask.model.logic;

public class KnightWorker {

//	public static Object copyArray(Knight knight, int arraySize) {
//		Knight newKnight = new Knight();
//		newKnight.setArraySize(arraySize);
//		for (int i = 0; i < knight.getArraySize(); i++) {
//			newKnight.getEquipments()[i] = knight.getEquipment(knight, i);
//		}
//
//		return newKnight;
//	}
	/*
	 * public static Knight increasingEnteringSort(Knight knight) { for (int i = 1;
	 * i < knight.getArraySize(); i++) { while (i > 0 && knight.getElement(i) <
	 * vector.getElement(i-1)) { double temp = vector.getElement(i-1);
	 * vector.setElement(i-1, vector.getElement(i)); vector.setElement(i, temp);
	 * i--; } } return vector; }
	 */
}
